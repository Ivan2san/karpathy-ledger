# Keep full local copies of source texts out of the public repo (copyright).
# Put offline reading copies here if you want them for local agent ingest.
raw/_local/
*.local.md

# OS / editor cruft
.DS_Store
.obsidian/

# Outputs: authored briefings stay private; only the README is tracked
# so the layer's intent is documented in the public repo.
outputs/*
!outputs/README.md
